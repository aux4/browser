# browser content

Get page content in the specified format.

## Usage

```
aux4 browser content --session <id> [--selector <css>] [--format markdown]
```

## Options

- `--session` — Session ID (required)
- `--selector` — CSS selector for specific element (default: full page)
- `--format` — Output format: markdown, html, text (default: markdown)
