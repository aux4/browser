# browser click

Click an element by accessible name and ARIA role.

## Usage

```
aux4 browser click --session <id> --name <name> [--role button]
```

## Options

- `--session` — Session ID (required)
- `--name` — Accessible name of the element (required)
- `--role` — ARIA role (default: button)
